From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: SucIXR <sasasa123456@foxmail.com>
Date: Mon, 22 Dec 2025 18:58:03 +0800
Subject: [PATCH] Mob_AI_Scheduler


diff --git a/net/minecraft/server/level/ServerLevel.java b/net/minecraft/server/level/ServerLevel.java
index 755fe00ea86a57aa899c19316d3dd90900d8e973..4b7c0ca50555984bc6bf31fce6f7900ce6c40013 100644
--- a/net/minecraft/server/level/ServerLevel.java
+++ b/net/minecraft/server/level/ServerLevel.java
@@ -761,6 +761,11 @@ public class ServerLevel extends Level implements ServerEntityGetter, WorldGenLe
     }
 
     public void tick(BooleanSupplier hasTimeLeft, io.papermc.paper.threadedregions.TickRegions.TickRegionData region) { // Folia - regionised ticking
+        // Kitin start - 每刻开始时更新性能预算管理器
+        // 获取当前层（Level）的最近 MSPT
+        long mspt = (long) (this.server.getAverageTickTimeNanos() / 1_000_000.0);
+        me.sucixr.kitin.scheduler.PerformanceBudgetManager.getInstance().onTickStart(mspt);
+        // Kitin end
         final io.papermc.paper.threadedregions.RegionizedWorldData regionizedWorldData = this.getCurrentWorldData(); // Folia - regionised ticking
         final ca.spottedleaf.leafprofiler.RegionizedProfiler.Handle profiler = io.papermc.paper.threadedregions.TickRegionScheduler.getProfiler(); // Folia - profiler
         ProfilerFiller profilerFiller = Profiler.get();
diff --git a/net/minecraft/world/entity/LivingEntity.java b/net/minecraft/world/entity/LivingEntity.java
index d5bd641094730571eb65110c84b1df287288be3d..45d4ebf2dade04deb1f62be02205055ea8b48628 100644
--- a/net/minecraft/world/entity/LivingEntity.java
+++ b/net/minecraft/world/entity/LivingEntity.java
@@ -3618,6 +3618,14 @@ public abstract class LivingEntity extends Entity implements Attackable, Waypoin
     }
 
     public void aiStep() {
+        // --- Kitin 优化注入开始 ---
+        // 根据服务器压力和玩家距离，决定是否跳过这一 tick 的 AI 处理
+        if (!me.sucixr.kitin.scheduler.PerformanceBudgetManager.getInstance().shouldProcessAI(this)) {
+            // 如果被拦截，只执行基础的物理移动（重力、水流等），跳过复杂的 AI 决策
+            this.setNoActionTime(this.getNoActionTime() + 1); // 保持基础状态更新
+            return;
+        }
+        // --- Kitin 优化注入结束 ---
         if (this.noJumpDelay > 0) {
             this.noJumpDelay--;
         }
