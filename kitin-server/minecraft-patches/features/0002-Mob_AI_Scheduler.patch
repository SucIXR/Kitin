From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: SucIXR <sasasa123456@foxmail.com>
Date: Mon, 22 Dec 2025 18:58:03 +0800
Subject: [PATCH] Mob_AI_Scheduler


diff --git a/net/minecraft/server/level/ServerLevel.java b/net/minecraft/server/level/ServerLevel.java
index 755fe00ea86a57aa899c19316d3dd90900d8e973..4b7c0ca50555984bc6bf31fce6f7900ce6c40013 100644
--- a/net/minecraft/server/level/ServerLevel.java
+++ b/net/minecraft/server/level/ServerLevel.java
@@ -761,6 +761,11 @@ public class ServerLevel extends Level implements ServerEntityGetter, WorldGenLe
     }
 
     public void tick(BooleanSupplier hasTimeLeft, io.papermc.paper.threadedregions.TickRegions.TickRegionData region) { // Folia - regionised ticking
+        // Kitin start - 每刻开始时更新性能预算管理器
+        // 获取当前层（Level）的最近 MSPT
+        long mspt = (long) (this.server.getAverageTickTimeNanos() / 1_000_000.0);
+        me.sucixr.kitin.scheduler.PerformanceBudgetManager.getInstance().onTickStart(mspt);
+        // Kitin end
         final io.papermc.paper.threadedregions.RegionizedWorldData regionizedWorldData = this.getCurrentWorldData(); // Folia - regionised ticking
         final ca.spottedleaf.leafprofiler.RegionizedProfiler.Handle profiler = io.papermc.paper.threadedregions.TickRegionScheduler.getProfiler(); // Folia - profiler
         ProfilerFiller profilerFiller = Profiler.get();
diff --git a/net/minecraft/world/entity/monster/zombie/Zombie.java b/net/minecraft/world/entity/monster/zombie/Zombie.java
index feccb1180618ec7a887c45033f0374e3d5d40f0b..84712b22c5a54f18f5a2a15b7fe81d99171cb559 100644
--- a/net/minecraft/world/entity/monster/zombie/Zombie.java
+++ b/net/minecraft/world/entity/monster/zombie/Zombie.java
@@ -211,6 +211,17 @@ public class Zombie extends Monster {
 
     @Override
     public void tick() {
+        // --- Kitin Start: 彻底拦截 AI 步进 ---
+        if (this.level() instanceof ServerLevel && !this.isNoAi()) {
+            // 如果调度器判定该实体不需要处理 AI
+            if (!me.sucixr.kitin.scheduler.PerformanceBudgetManager.getInstance().shouldProcessAI(this)) {
+                // 我们直接调用 LivingEntity 的超级父类 tick，只跑基础位移/物理逻辑
+                // 这会跳过 Mob 层的 AI 决策和 Zombie 层的溺水逻辑
+                super.tick();
+                return;
+            }
+        }
+        // --- Kitin End ---
         if (this.level() instanceof ServerLevel serverLevel && this.isAlive() && !this.isNoAi()) {
             if (this.isUnderWaterConverting()) {
                 this.conversionTime--;
diff --git a/net/minecraft/world/entity/monster/zombie/ZombifiedPiglin.java b/net/minecraft/world/entity/monster/zombie/ZombifiedPiglin.java
index 8fcb6cd1cefa6c9e1809944b4720cfdc35325729..d27db3e7a91027f59b1a888b12ec04cf17d0e173 100644
--- a/net/minecraft/world/entity/monster/zombie/ZombifiedPiglin.java
+++ b/net/minecraft/world/entity/monster/zombie/ZombifiedPiglin.java
@@ -122,6 +122,12 @@ public class ZombifiedPiglin extends Zombie implements NeutralMob {
     }
 
     private void maybeAlertOthers() {
+        // --- Kitin Start
+        if (this.level() instanceof ServerLevel &&
+                !me.sucixr.kitin.scheduler.PerformanceBudgetManager.getInstance().shouldProcessAI(this)) {
+            return;
+        }
+        // --- Kitin End ---
         if (this.ticksUntilNextAlert > 0) {
             this.ticksUntilNextAlert--;
         } else {
diff --git a/net/minecraft/world/entity/npc/villager/Villager.java b/net/minecraft/world/entity/npc/villager/Villager.java
index 8616a8f5b720eac2bb5b3d78a3b69d40bb0b5547..1cadfd2cefedb1a462b35ffb2717af3df648c47c 100644
--- a/net/minecraft/world/entity/npc/villager/Villager.java
+++ b/net/minecraft/world/entity/npc/villager/Villager.java
@@ -295,10 +295,17 @@ public class Villager extends AbstractVillager implements ReputationEventHandler
     }
     protected void customServerAiStep(ServerLevel level, final boolean inactive) {
         // Paper end - EAR 2
-        ProfilerFiller profilerFiller = Profiler.get();
-        profilerFiller.push("villagerBrain");
-        if (!inactive) this.getBrain().tick(level, this); // Paper - EAR 2
-        profilerFiller.pop();
+        // --- Kitin Start: 注入调度引擎 ---
+        // 如果原本 Paper 认为它是 active (inactive=false)，我们再过一遍概率引擎
+        boolean kitinInactive = inactive || !me.sucixr.kitin.scheduler.PerformanceBudgetManager.getInstance().shouldProcessAI(this);
+        if (!kitinInactive) {
+                this.getBrain().tick(level, this);
+        }
+        // --- Kitin End ---
+//        ProfilerFiller profilerFiller = Profiler.get();
+//        profilerFiller.push("villagerBrain");
+//        if (!inactive) this.getBrain().tick(level, this); // Paper - EAR 2
+//        profilerFiller.pop(); //Kitin off
         if (this.assignProfessionWhenSpawned) {
             this.assignProfessionWhenSpawned = false;
         }
@@ -332,7 +339,7 @@ public class Villager extends AbstractVillager implements ReputationEventHandler
             this.stopTrading();
         }
 
-        if (inactive) return; // Paper - EAR 2
+        if (kitinInactive) return; // Paper - EAR 2 //Kitin
         super.customServerAiStep(level);
     }
 
