From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: SucIXR <sasasa123456@foxmail.com>
Date: Sat, 20 Dec 2025 20:22:57 +0800
Subject: [PATCH] FoliaFix_WaypointManager


diff --git a/net/minecraft/server/level/ServerLevel.java b/net/minecraft/server/level/ServerLevel.java
index 7aa9a76f1063dbfef5f4c02746e0a679caac3cb5..34e66488f32ed5630cd8e475618c37d350985d55 100644
--- a/net/minecraft/server/level/ServerLevel.java
+++ b/net/minecraft/server/level/ServerLevel.java
@@ -649,7 +649,7 @@ public class ServerLevel extends Level implements ServerEntityGetter, WorldGenLe
         this.sleepStatus = new SleepStatus();
         this.gameEventDispatcher = new GameEventDispatcher(this);
         this.randomSequences = Objects.requireNonNullElseGet(randomSequences, () -> this.getDataStorage().computeIfAbsent(RandomSequences.TYPE));
-        this.waypointManager = new ServerWaypointManager();
+        this.waypointManager = new me.sucixr.kitin.fixes.foliafix.waypoint.KitinServerWaypointManager(this);
         this.environmentAttributes = EnvironmentAttributeSystem.builder().addDefaultLayers(this).build();
         //this.updateSkyBrightness(); // Folia - region threading - delay until first tick
         // Paper start - rewrite chunk system
diff --git a/net/minecraft/server/level/ServerPlayer.java b/net/minecraft/server/level/ServerPlayer.java
index 5d4b1542143a2a6467e1bfa9d21ffe58facede8a..9b0e4e0d0555dcaf1c4323dec7057235588877a0 100644
--- a/net/minecraft/server/level/ServerPlayer.java
+++ b/net/minecraft/server/level/ServerPlayer.java
@@ -875,6 +875,9 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
 
     @Override
     public void tick() {
+        // Kitin start
+        this.kitin$drainQueuedTasks();
+        // Kitin end
         // CraftBukkit start
         if (this.joining) {
             this.joining = false;
@@ -3639,5 +3642,37 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
     public org.bukkit.craftbukkit.entity.CraftPlayer getBukkitEntity() {
         return (org.bukkit.craftbukkit.entity.CraftPlayer) super.getBukkitEntity();
     }
-    // CraftBukkit end
+    // CraftBukkit
+
+    // Kitin start - player mailbox for region-safe callbacks
+    private final java.util.concurrent.ConcurrentLinkedQueue<Runnable> kitin$queuedTasks =
+            new java.util.concurrent.ConcurrentLinkedQueue<>();
+
+    public void kitin$scheduleToOrRun(final Runnable task) {
+        // 只要当前线程就是这个 player 的 tick thread，就直接跑
+        if (ca.spottedleaf.moonrise.common.util.TickThread.isTickThreadFor(this)) {
+            task.run();
+            return;
+        }
+        // 否则丢进邮箱，等 player 自己 tick 时处理
+        this.kitin$queuedTasks.add(task);
+    }
+
+    private void kitin$drainQueuedTasks() {
+        // 防止恶意/意外塞爆：每 tick 限量执行
+        for (int i = 0; i < 1024; i++) {
+            final Runnable r = this.kitin$queuedTasks.poll();
+            if (r == null) {
+                return;
+            }
+            try {
+                r.run();
+            } catch (Throwable t) {
+                // 这里用你项目里现有的 logger 方式即可
+                // 不要用 MinecraftServer.getLogger()（你已经踩过那个坑）
+                com.mojang.logging.LogUtils.getLogger().error("[Kitin] queued task error", t);
+            }
+        }
+    }
+    // Kitin end
 }
