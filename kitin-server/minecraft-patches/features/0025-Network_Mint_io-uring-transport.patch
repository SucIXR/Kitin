From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: SucIXR <sasasa123456@foxmail.com>
Date: Tue, 24 Feb 2026 23:49:14 +0800
Subject: [PATCH] Network_Mint_io uring transport

add io_uring event loop group and channel selection for remote connections
The IOUring transmission feature is only available on Linux platforms with kernel versions greater than 5.1

Co-authored by: Bacteriawa <A3167717663@hotmail.com>
As part of: Mint (https://github.com/MenthaMC/Mint/blob/e921ac17b0fb7ea8ca4788e269f2b23c51102e27/mint-server/minecraft-patches/features/0081-io_uring-transport.patch)
Licensed under: GPL-3.0 (https://github.com/MenthaMC/Mint/blob/84d3104175d78c80145789d7dcae52fe43cc5a29/licenses/GPL.md)

diff --git a/net/minecraft/network/Connection.java b/net/minecraft/network/Connection.java
index c26e81b07f1ea6d0820147da35cc8555fd83f75c..90114b681d66d46d53ccdfb3bf39bb52066172f4 100644
--- a/net/minecraft/network/Connection.java
+++ b/net/minecraft/network/Connection.java
@@ -186,6 +186,9 @@ public class Connection extends SimpleChannelInboundHandler<Packet<?>> {
 
     @Override
     public void exceptionCaught(ChannelHandlerContext ctx, Throwable exception) {
+        // Mint start - mirror ModernNetty exception logging behavior
+        LOGGER.warn("netty exception", exception);
+        // Mint end - mirror ModernNetty exception logging behavior
         // Paper start - Handle large packets disconnecting client
         if (exception instanceof io.netty.handler.codec.EncoderException && exception.getCause() instanceof PacketEncoder.PacketTooLargeException packetTooLargeException) {
             final Packet<?> packet = packetTooLargeException.getPacket();
diff --git a/net/minecraft/server/network/EventLoopGroupHolder.java b/net/minecraft/server/network/EventLoopGroupHolder.java
index c602146d15f16007ee2499d0a68670d0d630bfbb..21edf59f1952ebb57d263e81cfc08543fc32004e 100644
--- a/net/minecraft/server/network/EventLoopGroupHolder.java
+++ b/net/minecraft/server/network/EventLoopGroupHolder.java
@@ -20,6 +20,10 @@ import io.netty.channel.local.LocalServerChannel;
 import io.netty.channel.nio.NioIoHandler;
 import io.netty.channel.socket.nio.NioServerSocketChannel;
 import io.netty.channel.socket.nio.NioSocketChannel;
+import io.netty.channel.uring.IoUring;
+import io.netty.channel.uring.IoUringIoHandler;
+import io.netty.channel.uring.IoUringServerSocketChannel;
+import io.netty.channel.uring.IoUringSocketChannel;
 import java.util.concurrent.ThreadFactory;
 import org.jspecify.annotations.Nullable;
 
@@ -42,6 +46,14 @@ public abstract class EventLoopGroupHolder {
             return KQueueIoHandler.newFactory();
         }
     };
+    // Mint start - io_uring transport
+    private static final EventLoopGroupHolder IO_URING = new EventLoopGroupHolder("IOUring", IoUringSocketChannel.class, IoUringServerSocketChannel.class) {
+        @Override
+        protected IoHandlerFactory ioHandlerFactory() {
+            return IoUringIoHandler.newFactory();
+        }
+    };
+    // Mint end - io_uring transport
     private static final EventLoopGroupHolder LOCAL = new EventLoopGroupHolder("Local", LocalChannel.class, LocalServerChannel.class) {
         @Override
         protected IoHandlerFactory ioHandlerFactory() {
@@ -64,6 +76,11 @@ public abstract class EventLoopGroupHolder {
     @Deprecated @io.papermc.paper.annotation.DoNotUse // Paper - use variant with address param
     public static EventLoopGroupHolder remote(boolean tryNativeTransport) {
         if (tryNativeTransport) {
+            // Mint start - io_uring transport
+            if (IoUring.isAvailable()) {
+                return IO_URING;
+            }
+            // Mint end - io_uring transport
             if (KQueue.isAvailable()) {
                 return KQUEUE;
             }
@@ -79,6 +96,11 @@ public abstract class EventLoopGroupHolder {
     // Paper start - Unix domain socket support
     public static EventLoopGroupHolder remote(java.net.SocketAddress address, boolean tryNativeTransport) {
         if (tryNativeTransport) {
+            // Mint start - io_uring transport
+            if (!(address instanceof io.netty.channel.unix.DomainSocketAddress) && IoUring.isAvailable()) {
+                return IO_URING;
+            }
+            // Mint end - io_uring transport
             if (KQueue.isAvailable()) {
                 return KQUEUE;
             }
