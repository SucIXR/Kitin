From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: SucIXR <sasasa123456@foxmail.com>
Date: Fri, 24 Jul 2026 12:00:00 +0800
Subject: [PATCH] Perf_VillagerSmartHibernation


diff --git a/net/minecraft/world/entity/npc/villager/Villager.java b/net/minecraft/world/entity/npc/villager/Villager.java
index 07677108d5f11057f6bd5a02bda4e49d3b802869..6db45f54175362af80ee9dd22eca65542d5972c0 100644
--- a/net/minecraft/world/entity/npc/villager/Villager.java
+++ b/net/minecraft/world/entity/npc/villager/Villager.java
@@ -171,6 +171,7 @@ public class Villager extends AbstractVillager implements VillagerDataHolder, Re
             return activities;
         }
     );
+    public net.minecraft.world.level.block.state.BlockState[] kitin$prisonSnapshot = null; // Kitin - VillagerSmartHibernation
     public static final Map<MemoryModuleType<GlobalPos>, BiPredicate<Villager, Holder<PoiType>>> POI_MEMORIES = ImmutableMap.of(
         MemoryModuleType.HOME,
         (villager, poiType) -> poiType.is(PoiTypes.HOME),
@@ -270,6 +271,7 @@ public class Villager extends AbstractVillager implements VillagerDataHolder, Re
     }
     protected void customServerAiStep(ServerLevel level, final boolean inactive) {
         // Paper end - EAR 2
+        if (me.sucixr.kitin.performance.entity.villager.VillagerSmartHibernation.checkFreezeTick(this, level)) return; // Kitin - VillagerSmartHibernation
         ProfilerFiller profiler = Profiler.get();
         profiler.push("villagerBrain");
         if (!inactive) this.getBrain().tick(level, this); // Paper - EAR 2
@@ -309,6 +311,7 @@ public class Villager extends AbstractVillager implements VillagerDataHolder, Re
 
         if (inactive) return; // Paper - EAR 2
         super.customServerAiStep(level);
+        me.sucixr.kitin.performance.entity.villager.VillagerSmartHibernation.attemptCapture(this, level); // Kitin - VillagerSmartHibernation
     }
 
     @Override
